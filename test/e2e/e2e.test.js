const request = require('supertest')
const app = require('../../server/server')

describe('Memorize messages', () => {
  test('it should answer `อยากให้ช่วยจำอะไรอีก พิมพ์มาได้เลยครับ! ถ้้าพอแล้ว ให้พิมพ์มาว่า `พอ``', () => {

  })
})